{
  "id": "occluding-modal",
  "description": "A full-viewport 'What's new' release-notes modal covers the checkout button on load, so the flow's click on the (correctly selected) button times out.",
  "category": "occluding_overlay",
  "expected_failure_type": "selector_not_visible",
  "expected_finding": "blocking_overlay",
  "fix_scope": "config",
  "max_iterations": 5,
  "invariants": [
    { "file": "app/server.js", "must_not_change": true },
    { "file": "verfix.config.json", "must_contain": "\"type\": \"text_visible\"" }
  ]
}
