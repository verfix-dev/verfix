.idea/
.vscode/
*.log
# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
**/dist/
.next/

# Go binaries
api/verfix-api

# Artifacts
workers/artifacts/

# Environment
.env
**/.env

# OS
.DS_Store

/ignore
# Marketing drafts and launch plans — local only, repo is public
/marketing
**/.kilo
**/.verfix
# Exception: committed benchmark case fixtures (pre-baked storage state) live
# under a case's own .verfix/ — these are test data, not runtime output.
!benchmark/cases/**/.verfix
!benchmark/cases/**/.verfix/**
